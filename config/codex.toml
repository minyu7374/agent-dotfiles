# agent-dotfiles 托管段(config/codex.toml -> ~/.codex/config.toml,由 scripts/sync.sh 写入)
# Codex 没有 Claude 的 plugin 概念,这里只承载行为级配置;能力靠 skills(见 SKILLS.md)。
model_reasoning_effort = "high"
disable_response_storage = true
